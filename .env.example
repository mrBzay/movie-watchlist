# Required connection for the API routes
DATABASE_URL="postgres://user:password@host:5432/database"

# Optional: direct connection string for migrations/seed scripts
DIRECT_URL="postgres://user:password@host:5432/database"

# Optional: default user email used when no auth is implemented
DEFAULT_USER_EMAIL="demo@example.com"
